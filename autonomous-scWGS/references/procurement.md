# Functional planning

The public checklist names capability classes only. Every line is non-orderable
until a controlled local profile defines a qualified selection and quantity.

The project performs no storefront checkout and exposes no supplier catalog data.
