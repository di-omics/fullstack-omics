# WGS analysis handoff

The generator writes a sample manifest and a shell runner. The runner requires a
compatible external pipeline checkout and execution profile at runtime.

The repository does not bundle, fetch, or prescribe an analysis implementation.
