# autonomous-scWGS runtime dependencies
pylabrobot>=0.2.0     # Hamilton STAR + thermocycler (ODTC) + Synergy H1 backends & simulator
pyyaml>=6.0           # config/*.yaml (source of truth)

# Optional:
#   pandoc (system package)  -> Markdown -> PDF for the bench manual
#   pytest                   -> `pytest -q` (tests also run via plain python)
#
# Stage 4 analysis calls these EXTERNAL tools (install separately, not pip):
#   bcl2fastq (Illumina), bwa-mem2 (or bwa), samtools, mosdepth, deepvariant/gatk (deep).
#   Vendor path: the vendor WGS analysis (hosted platform -- no local install).
