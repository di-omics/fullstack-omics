# Architecture

```text
public functional checklist
        |
        v
abstract workflow-state graph
        |
        v
unitless deterministic simulation
        |
        v
runtime-configured analysis-adapter handoff
```

The ignored local validated profile is a separate boundary. It may name a
laboratory-owned adapter, but no physical method data is committed here.
